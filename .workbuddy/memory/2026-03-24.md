# 工作日志 - 2026-03-24

## 基本面非制造业PMI展示

**需求**: 宏观供需关系模块增加非制造业PMI，格式同制造业PMI

**feishu.py 改动**:
- 新增提取 `pmi_svc` 字段（数据层已有）
- 新增解读逻辑：≥50=🟢扩张，45-50=🟡临界偏弱，<45=🔴收缩
- 新增展示行：`非制造业PMI **49.5** → 🟡临界偏弱（45-50）`

**测试结果**: 非制造业PMI 49.5 🟡临界偏弱，飞书已发送 ✓

---

## 新增宏观交易分析报告HTML输出

**需求**: 新增 `--macro` 命令行选项，生成 HTML 格式的宏观交易分析报告

**实现内容**:

### 1. 新建 `market_monitor/report/macro_report.py`

`build_html_report()`: 构建完整HTML报告，包含：
- **头部**: 报告标题和生成时间
- **市场概览**: 4个关键指标卡片（万得全A PE、指南针活跃市值、两融余额、七巨头PE）
- **资金面**: 
  - 核心资金观察指标
  - 资金流入渠道（散户、杠杆、机构、外资）
  - 资金流出渠道
- **基本面**: 
  - 宏观经济层（GDP、CPI、PPI、PMI、M2、国债）
  - 估值层（PE/PB/股息率仪表盘）
- **政策面**: 货币政策指标（待接入）
- **全球市场**: 美股估值/走势、港股、大宗商品、外汇
- **综合判断**: 四维分析卡片和交易提示

`save_html_report()`: 保存到本地文件  
`generate_and_save()`: 完整生成流程

### 2. 修改 `main.py`

- 导入 `macro_report` 模块
- 添加 `--macro` 命令行参数解析
- 新增 Step 8：生成HTML报告并输出文件路径

**用法**:
```bash
python3 -m market_monitor --macro           # 仅生成HTML报告
python3 -m market_monitor --macro --feishu  # 同时生成HTML并发送飞书
```

**输出路径**: `market_monitor/data/macro_report_YYYY-MM-DD.html`

**测试结果**: ✓ HTML报告生成成功，飞书已发送

---

## HTML报告资金面模块优化（2026-03-24）

**需求**: 调整HTML报告资金面指标格式及内容

**改动内容**:

### 1. 指南针活跃市值数据源
- 数据源列显示"手动上传"链接
- 点击后弹出模态框，包含三个输入框：
  - 活跃市值点数（亿元）
  - 涨跌幅（%）
  - 数据日期（支持快捷选择：昨天/今天/一周前）
- 确认后更新页面展示（表格和概览卡片）
- 支持修改（可重复点击上传）

### 2. 资金流入渠道结构调整
分为五部分：
- **散户资金**: 展示A股月度新开户数（展开状态）
- **杠杆资金**: 展示两融余额、两融余额占流通市值、两融交易额占比（展开状态）
- **机构资金**: 折叠状态，包含公募基金新发规模、保险资金、社保基金（待接入）
- **外资**: 折叠状态，包含北向资金净流入、累计净买入、每日成交额（待接入）
- **分红再投资**: 折叠状态，包含年度分红总额、股息再投资流入、上市公司回购规模（待接入）

### 3. 资金流出渠道
显示提示信息："⚠️ 资金流出指标（IPO融资、再融资、股东减持等）暂无实时数据源接入"

**技术实现**:
- 新增CSS样式：模态框、折叠面板、表单样式
- 新增JavaScript功能：
  - `toggleCollapsible()`: 折叠/展开面板
  - `showManualUpload()`: 显示上传模态框
  - `setDate()`: 快捷日期设置
  - `saveManualData()`: 保存数据并更新页面

**测试结果**: ✓ HTML报告生成成功，浏览器预览正常
