## 2026-03-23 工作记录

### 修复全球市场飞书展示 key 不匹配问题

**问题**: 飞书报告全球市场板块显示"数据获取中"，实际数据正常获取

**根因**: feishu.py 中读取全球市场数据时使用了错误的 key

**修复内容**: 统一用 `"us"`, `"asia"`, `"forex"` 及 `"price"`, `"chg5d_pct"` 字段

---

### 全球市场模块大幅扩展

**需求**: 分市场展示、新增MAGS/TECHK估值、恒生科技、韩国指数

**global_mkt.py 改动**:
1. 新增 `_calc_percentile`, `_fetch_wind_valuation_raw`, `_wind_parse_valuation`, `_wind_save_csv` 通用工具函数
2. 新增 `fetch_mags_valuation()` - MAGS.WI 七巨头指数估值，替代旧的 `fetch_us_valuation()`
3. 新增 `fetch_techk_valuation()` - TECHK.WI 港股中国科技龙头估值
4. 扩展 `fetch_asia_market()` - 增加恒生科技(HSTECH)、韩国综合(KOSPI)、韩国创业(KOSDAQ)
5. CSV保存路径: `data/wind_mags_pe_history.csv`, `data/wind_techk_pe_history.csv`

**main.py 改动**: global_data 结构更新，key: `"us"`, `"mags_val"`, `"commodities"`, `"forex"`, `"asia"`, `"techk_val"`

**feishu.py 改动**: 全球市场分国旗+市场分块展示，涨跌超3%加粗，估值用图标(🔴🟡🟢)

**测试结果（2026-03-20）**:
- MAGS.WI PE 30.7, 第7%（🟢，历史回算低估）
- TECHK.WI PE 21.2, 第20%（🟡，接近低估）
- KOSPI 韩国综合 5,435 5日 -2.1%
- 飞书已发送 ✓

---

### 全球市场消息格式进一步调整（2026-03-23）

**feishu.py 改动**:
1. `_build_summary` global 分支重写为趋势叙述句（↗/↘/→ 美股/港股趋势 + 黄金/美元信号）
2. 七巨头MAGS估值移到美股板块第一行，同行显示 PE+百分位 + PB+百分位
3. 中国科技龙头TECHK估值移到港股板块第一行，同行显示 PE+百分位 + PB+百分位
4. 消息末尾新增一行：「📱 更多指数估值及历史数据，可前往 Wind 手机版查看」（用hr分隔）

---

### 妙想 API 集成优化（2026-03-23）

**mx_search.py 改动**:
1. 改为直接调用妙想 API（`https://mkapi2.dfcfs.com/finskillshub/api/claw/news-search`），避免依赖有语法错误的脚本文件
2. 添加 6 小时本地缓存机制（`data/cache/mx_search_*.json`），减少 API 调用
3. 精简搜索关键词为 2 个核心词（"印花税 降低 减免 减半"、"券商佣金率 下调 优惠"）
4. 每次查询限制返回 top 2 条结果
5. 添加 `source` 字段标识数据来源（"API2次" / "缓存2个" / "API1次+缓存1个"等）

**main.py 改动**: 更新日志输出，显示 `[缓存N个]` 或 `[API+N次]` 标签

**限额控制**:
- 首次运行：2 次 API 调用
- 6 小时内重复运行：0 次 API 调用（全部命中缓存）
- 每次调用返回 2 条结果，最多存储 4 条资讯

---

### 资金面模块扩展：政策监控 + 港股回购减持 + IPO监控（2026-03-23）

**需求**: 增加印花税/佣金率政策监控、港股互联网巨头回购减持、百亿/千亿级IPO监控

**mx_search.py 改动**:
1. 新增 `POLICY_QUERIES` / `HK_REPO_QUERIES` / `IPO_QUERIES` 三组搜索词
2. 新增 `_analyze_policy_sentiment()` - 分析印花税/佣金率政策情绪
3. 新增 `_analyze_repo_sentiment()` - 分析回购/减持/融资事件情绪
4. 新增 `_analyze_ipo_sentiment()` - 分析IPO规模及情绪（识别千亿/百亿级、警示公司）
5. 新增 `fetch_policy()` - 获取印花税/佣金率政策消息
6. 新增 `fetch_hk_repo()` - 获取港股互联网巨头回购减持动态
7. 新增 `fetch_ipo()` - 获取百亿/千亿级IPO监控

**main.py 改动**:
- 新增调用 `mx_search_mod.fetch_hk_repo()` 和 `mx_search_mod.fetch_ipo()`
- 分别存入 `capital_data["hk_repo"]` 和 `capital_data["ipo_monitor"]`
- 日志显示 API 调用次数和缓存命中情况

**feishu.py 改动**（资金面板块新增三个子模块）：
1. **政策监控（交易费用）**：
   - 展示印花税/佣金率状态（利好/利空/中性）
   - 说明："印花税调整是政策信号：降低=刺激股市，提高=降温"

2. **港股科技龙头回购减持**：
   - 展示回购注销、回购、减持、融资分类统计
   - 说明："回购注销=看好公司，减持=利空，巨量融资=见顶信号"

3. **IPO/再融资监控**：
   - 识别百亿/千亿级IPO（长鑫科技、长江存储、荣耀等）
   - 巨量IPO启动显示 ⚠️ 警示
   - 说明："牛市后期巨量融资是见顶信号，监管通过融资节奏调节股市"

**限额控制**（扩展后）：
- 印花税/佣金率：2 次查询
- 港股回购减持：2 次查询  
- IPO监控：2 次查询
- 总计最多 6 次 API 调用（首次运行），6 小时内缓存命中则 0 次

**测试结果**（2026-03-23）:
- 印花税/佣金率：✓ 中性 - 中性4条... [缓存2个]
- 港股回购减持：✓ 偏空 - 🟢回购注销1条 | 🔴减持2条... [API2次]
- IPO/再融资：✓ 偏空 - IPO/再融资活跃，注意资金分流... [API2次]

---

### 指南针活跃市值模块（2026-03-23）

**需求**: 
1. 增加指南针活跃市值手动输入保存功能，保存至 CSV
2. 2026/3/23 数据：活跃市值 186349.4 亿，下跌 2.94%
3. 信号规则：单日涨幅≥4%=增量入场，单日跌幅≤-2.3%=资金离场
4. 动态仓位建议：0-40%

**capital.py 改动**:
1. 新增 `_ZNZ_CSV_PATH` / `_ZNZ_CSV_FIELDS` 配置
2. 新增 `_read_znz_csv()` / `_write_znz_csv()` CSV 读写函数
3. 新增 `_calc_znz_signal()` - 根据涨跌幅计算信号
4. 新增 `_suggest_position()` - 根据信号给出仓位建议
5. 新增 `save_znz_active_cap(date, active_cap, chg_pct)` - 手动录入并保存
6. 新增 `fetch_znz_active_cap()` - 读取最新数据并返回完整信息

**main.py 改动**:
1. 新增 `--znz` 命令行参数，格式：`--znz "YYYY-MM-DD,active_cap,chg_pct"`
2. 新增 `znz_override` 配置解析
3. 资金面采集步骤新增指南针活跃市值采集（优先级最高）
4. 日志输出显示信号和仓位建议

**feishu.py 改动**:
1. 资金面展示新增指南针活跃市值区块（放在第一位）
2. 显示：日期、活跃市值、涨跌幅、信号图标、信号描述、仓位建议
3. 添加信号规则说明
4. `_build_summary()` 资金面总结逻辑重写：
   - 优先看指南针信号（增量入场/资金离场）
   - 辅助看两融趋势（杠杆回暖/降温）
   - 再看新开户情绪（过热/低迷/升温/降温）
   - 最后看政策/IPO信号
   - 组合成一句话总结，用 `|` 分隔

**CSV 文件**: `market_monitor/data/znz_active_cap.csv`
- 字段：date, active_cap, chg_pct, signal, source

**测试结果**（2026-03-23）:
```
指南针活跃市值 ↩ 手动录入 186349.4 亿... 
✓  [2026-03-23] 186,349.4亿 -2.94% → 🔴 资金离场警示（单日跌幅≤-2.3%） 建议仓位0-10%

资金面总结：🔴资金离场警示 | 杠杆降温 | 散户情绪降温
```

---

### 资金面删除三个数据质量差的模块（2026-03-23）

因妙想 API 搜索数据准确性较差，删除资金面以下三个模块：
- 政策监控（交易费用）- 印花税/佣金率
- 港股科技龙头 - 回购减持监控
- IPO/再融资监控

**main.py**: 移除 `mx_search_mod` 导入及三个模块的采集代码
**feishu.py**: 移除三个模块的展示代码（约80行）

---

### 基本面非制造业PMI展示（2026-03-24）

**需求**: 宏观供需关系模块增加非制造业PMI，格式同制造业PMI

**feishu.py 改动**:
- 新增提取 `pmi_svc` 字段（数据层已有）
- 新增解读逻辑：≥50=🟢扩张，45-50=🟡临界偏弱，<45=🔴收缩
- 新增展示行：`非制造业PMI **49.5** → 🟡临界偏弱（45-50）`

**测试结果**（2026-03-24）: 非制造业PMI 49.5 🟡临界偏弱，飞书已发送 ✓

